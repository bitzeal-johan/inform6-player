import { type TerminalConfig } from './types/terminal-config.js';
/**
 * Default terminal configuration with three breakpoints:
 * - Mobile (0px+): 40 columns, smaller font range
 * - Tablet (600px+): 60 columns, medium font range
 * - Desktop (1024px+): 80 columns, larger font range
 */
export declare const DEFAULT_TERMINAL_CONFIG: TerminalConfig;
